/*
//array ... operator
const fruits = ["apple", "banan", "orange"];
const vegitables = ["cucumber", "radish"];

console.log([...fruits, ...vegitables]);
*/

/*
//about objects
const developer = {
  salary: 98000,
  experience: 4.5,
  trechStack: ["vue", "html", "css", "javascript", "java"],
  lookingForWork: true,
  doubleSalary() {
    this.salary = this.salary * 2;
    this.lookingForWork = false;
  },
};

console.log(developer.salary);
console.log(developer.lookingForWork);

developer.doubleSalary();

console.log(developer.salary);
console.log(developer.lookingForWork);
*/

/*
export const evenOrOdd = (number) => {
  if (number % 2 === 0) {
    return "Even";
  } else {
    return "Odd";
  }
};

export const multiply = (num1, num2) => {
  /*let total = 0;

  for (let i = 1; i <= num1; i++) {
    total += num2;
  }
  return total;
  */
/*
  return num1 * num2;
};
*/
